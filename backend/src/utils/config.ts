// Configuration interface and default export

interface Config {
  // Add specific configuration properties as needed
  [key: string]: any;
}

// Export configuration values
const config: Config = {
  // Your configuration values
};

export default config;
